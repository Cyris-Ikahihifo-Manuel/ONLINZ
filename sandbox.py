# the decorations function makes it so that the program displays a certain message


def decorations(message, decoration):
    messages = [(6 * decoration) + (len(message) * decoration) (3 * decoration) + message + (3 * decoration)]



# the message function outputs


def message(message):
    print()
    print(message)
